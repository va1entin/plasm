@@ -24,7 +24,6 @@
 import urllib
 import urllib2
 import pycurl
-from plasm import encrypt
 
 import settings
 import utils
@@ -874,7 +873,6 @@
         return logging.error('service "%s" not initialized for camera with id %s' % (service_name, camera_id))
 
     try:
-        filename = encrypt.encrypt(filename, '/etc/plasm/public.key', removeInputFile=True)
         service.upload_file(target_dir, filename)
 
     except Exception as e:
